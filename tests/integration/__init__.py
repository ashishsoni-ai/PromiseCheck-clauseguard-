"""Integration tests - wire multiple steps together.

These may require a running agent-under-test container or a live LLM provider,
in which case they carry `@pytest.mark.live` or `@pytest.mark.integration`.
"""
