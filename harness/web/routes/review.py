"""Rule review screen — accept/edit/reject each rule (DESIGN.md 1.2). Deferred —
the review process this would serve is already a CLI workflow (`clauseguard report`
generates the reviewable artefact; the lockfiles are human-authored and reviewed
by policy). A web review queue would be nice but is not load-bearing for the
published numbers, so it stays a documented placeholder.
"""