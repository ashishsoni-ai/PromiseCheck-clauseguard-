"""Run-to-run diff screen (DESIGN.md 1.7). Deferred — the CLI serves this content
(`clauseguard run` prints the per-run history strip). A web diff view adds no data
the CLI does not already show and would not appear in the demo video, so it stays
a documented placeholder until there is a specific use case that requires it.
"""